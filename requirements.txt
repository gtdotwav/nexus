# NEXUS Gaming Agent - Dependencies
# NOTE: Preferred install method is `pip install -e ".[all]"` from pyproject.toml

# Perception
dxcam>=0.4.0          # DirectX screen capture (Windows)
opencv-python>=4.9.0  # Computer vision, template matching
numpy>=1.26.0         # Fast array operations
easyocr>=1.7.0        # OCR for game text/chat (legacy)
Pillow>=10.0.0        # Image manipulation

# Input Control
pynput>=1.7.6         # Keyboard and mouse control

# AI Engine
anthropic>=0.40.0     # Claude API client

# State & Config
pyyaml>=6.0           # Skill file parsing
pydantic>=2.5.0       # Data validation and settings

# Async & Performance
aiofiles>=24.0.0      # Async file I/O (consciousness, goals, mastery)
uvloop>=0.19.0;platform_system!='Windows'  # Fast event loop (Linux)

# Logging & Monitoring
structlog>=24.0.0     # Structured logging
rich>=13.0.0          # Beautiful terminal output

# Dashboard
aiohttp>=3.9.0        # HTTP + WebSocket server
aiohttp-cors>=0.7.0   # CORS support

# Data
sqlite-utils>=3.36    # SQLite convenience wrapper

# CLI
click>=8.1.0          # Command-line interface
